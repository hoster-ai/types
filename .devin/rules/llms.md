---
trigger: manual
---

LLM documentation conventions (`llm.txt` + `llm/`) live in the project skill:

**`.claude/skills/hoster-types/references/llm-docs.md`**

That file is the single source of truth — read it for the index format, per-item file format, the `@JSONSchema`-strip rule for code blocks, and the maintenance checklist.
